---
:backends:
    - yaml
:yaml:
    :datadir: "/etc/puppet/hieradata"
:eyaml:
:logger: console
:hierarchy:
  - "node/%{::hostname}"
  - "%{::environment}/%{::hardware}"
  - "%{::environment}/%{::role}"
  - "%{::environment}/common"
  - "%{::hardware}"
  - "%{::role}"
  - common
:merge_behavior: deeper
