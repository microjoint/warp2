---
:backends:
    - yaml
:yaml:
    :datadir: "/etc/puppet/hieradata"
:eyaml:
:logger: console
:hierarchy:
    - "node/%{hostname}"
    - "%{environment}/%{role}"
    - "%{environment}/%{common}"
    - "%{role}"
    - common
