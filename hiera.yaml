---
:backends:
    - yaml
:yaml:
    :datadir: "/etc/puppet/hieradata"
:eyaml:
:logger: console
:hierarchy:
  - "node/%{::hostname}"
  - "%{::environment}/%{::hardware}/%{::role}"
  - "%{::environment}/%{::hardware}/common"
  - "%{::environment}/%{::role}"
  - "%{::environment}/common"
  - "%{::hardware}"
  - "%{::role}"
  - common
